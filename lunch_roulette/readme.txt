LUNCH ROULETTE

1) Read entries from CSV
create instance of Onfidoer for every row from the CSV and store it in an array

2) class Onfidoer

 find the last non-zero digit of n!

3) Store groups 

4) If groups have less than the desired number then split its members between the other groups/across the first groups

5) Save in a CSV, each individual groups as a line

6) Given a group number print the unique teams of group memebers - ie if there is 2 engineers only print it once

